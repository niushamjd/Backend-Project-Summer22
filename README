This is an online library for which users can login and display or add books to the library.
The library uses a database that stores the books and their details along with user details.
To start the application you can type yarn dev in the terminal and have a postgres databse running containing two tables which are users and books table.